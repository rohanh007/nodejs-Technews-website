<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>News Article</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header class="header">
        <div class="header_logo">TechNews Website</div>
        <%- include('./partials/search.ejs') %>
    </header>

    <div class="wrapper">
        <div class="news-single">
            <a href="/">&larr; Back</a>

            <!-- Use EJS tags to display article details -->
            <% if(article != null) { %>
            <h2><%= article[0].title %></h2>
            <p><%= article[0].description %></p>
            <p><strong>Author:</strong> <%= article[0].author %></p>
            <p><strong>Published At:</strong> <%= article[0].publishedAt %></p>
            <img src="<%- article[0].urlToImage %>" alt="" class="article-image">
            <p><strong>Content:</strong></p>
            <div><%= article[0].content %></div>
            <!-- <p><strong>Source:</strong> <%= article[0].source %></p> -->
            <p><strong>URL:</strong> <a href="<%= article[0].url %>" target="_blank"><%= article[0].url %></a></p>

            <% } %>
        </div>
    </div>
</body>
</html>
